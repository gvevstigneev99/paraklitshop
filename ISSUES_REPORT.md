# Отчёт о недостатках проекта Paraklit Shop

## 🔴 Критические проблемы безопасности

### 1. Хардкод секретного ключа JWT
**Файл:** `internal/auth/login.go:9`
- Используется хардкод `"your_secret_key"` вместо значения из конфигурации
- Секретный ключ должен браться из `cfg.JWT.Secret`
- **Риск:** Компрометация токенов, если код попадет в публичный репозиторий

### 2. Хардкод userID в обработчиках
**Файлы:** 
- `internal/handler/cart.go:19,36,45,54`
- `internal/handler/order.go:28`
- Всегда используется `userID := 1` вместо получения из JWT токена
- **Риск:** Все пользователи работают с одной корзиной/заказами

### 3. Хардкод пароля для аутентификации
**Файл:** `internal/service/auth/auth.go:29`
- Проверка пароля `password123` захардкожена
- **Риск:** Отсутствие реальной аутентификации

### 4. Неиспользуемый middleware с хардкод токеном
**Файл:** `internal/middleware/auth.go:8`
- Существует `JWTAuthMiddleware` с хардкод токеном `"secret123"`
- Не используется, но может быть случайно подключен
- **Риск:** Путаница в коде, потенциальная уязвимость

### 5. Конфигурация содержит чувствительные данные
**Файл:** `config/local.yaml`
- Пароли и секреты хранятся в YAML файле
- Должны быть в переменных окружения или `.env` файле
- **Риск:** Утечка секретов при коммите в Git

## 🟠 Архитектурные проблемы

### 6. Отсутствие реальных подключений к БД
**Файлы:** 
- `internal/repository/postgres/product.go` - использует in-memory слайс
- `internal/repository/postgres/order.go` - использует in-memory слайс
- `internal/repository/redis/cart.go` - использует in-memory map вместо Redis
- Конфигурация PostgreSQL и Redis есть, но не используется
- **Проблема:** Данные теряются при перезапуске, нет персистентности

### 7. Дублирование логики аутентификации
**Файлы:**
- `internal/handler/login.go` - использует `auth.GenerateToken`
- `internal/handler/auth/handler.go` - использует `service/auth.AuthService`
- `internal/auth/login.go` - своя реализация JWT
- `internal/service/auth/auth.go` - другая реализация JWT
- **Проблема:** Две разные системы аутентификации, путаница

### 8. Неиспользуемые переменные
**Файл:** `internal/server/server.go:44-57`
- Множество переменных создаются с `_ = ...` для избежания ошибок компиляции
- Указывает на незавершенную рефакторинг или неправильную архитектуру

### 9. AuthService не инициализируется
**Файл:** `internal/server/server.go`
- `AuthService` определен, но не создается и не используется
- `handler.Login` использует старую реализацию из `internal/auth/login.go`

### 10. Неправильный импорт в auth handler (ОШИБКА КОМПИЛЯЦИИ)
**Файл:** `internal/handler/auth/handler.go:4`
- Используется относительный импорт `"internal/service/auth"` вместо `"paraklitshop/internal/service/auth"`
- **КРИТИЧНО:** Проект не компилируется из-за этой ошибки
- Должно быть: `"paraklitshop/internal/service/auth"`

## 🟡 Проблемы качества кода

### 11. Опечатка в сообщении об ошибке
**Файл:** `internal/service/auth/auth.go:30`
- `"invalid creditions"` должно быть `"invalid credentials"`

### 12. Дублирование middleware
**Файлы:**
- `internal/middleware/logging.go` и `internal/middleware/timing.go`
- Оба делают одно и то же - логируют время выполнения запроса
- Один из них избыточен

### 13. Order ID всегда 0
**Файл:** `internal/service/order.go:56`
- `ID: 0` - ID заказа не генерируется
- Должен быть автоинкремент или UUID

### 14. Отсутствие валидации входных данных
- Нет проверки на отрицательные количества товаров
- Нет проверки на существование товара перед добавлением в корзину (есть только в сервисе)
- Нет валидации параметров запросов

### 15. Несогласованная обработка ошибок
- В некоторых местах возвращается `error`, в других - `fiber.Ctx.JSON`
- Нет единого формата ошибок

### 16. Хардкод пути к конфигурации
**Файл:** `internal/config/config.go:55`
- Путь `"config/local.yaml"` захардкожен
- Должен поддерживать переменные окружения или разные окружения

## 🔵 Отсутствующие функции

### 17. Нет тестов
- Отсутствуют unit-тесты
- Отсутствуют integration-тесты
- Нет тестов для критичных компонентов (auth, orders)

### 18. Нет миграций БД
- Нет схемы базы данных
- Нет миграций для PostgreSQL
- Структуры моделей не соответствуют реальной БД

### 19. Отсутствует .gitignore
- Нет файла `.gitignore`
- Риск коммита чувствительных данных, бинарников, временных файлов

### 20. Отсутствует README
- Нет документации по установке
- Нет описания API
- Нет инструкций по запуску

### 21. Нет Docker конфигурации
- Отсутствует `Dockerfile`
- Отсутствует `docker-compose.yml`
- Усложняет развертывание

### 22. Неиспользуемые поля в конфигурации
**Файл:** `config/local.yaml`
- `http.host` - не используется (всегда `:port`)
- `redis.password` - не используется (в репозитории нет подключения к Redis)
- `logging.port` и `logging.host` - не используются

### 23. Неправильный порядок middleware
**Файл:** `internal/server/server.go:63-65`
- Middleware применяются после регистрации публичных роутов
- Должны применяться глобально до регистрации роутов или к конкретным группам

### 24. Дублирование роутов
**Файл:** `internal/server/server.go`
- Роуты `/api/cart` и `/buyer/cart` дублируются
- Роуты `/api/orders` и `/buyer/orders` дублируются
- Создает путаницу в API

### 25. Неправильные HTTP методы для некоторых операций
**Файл:** `internal/server/server.go:75`
- `POST /cart/add/:productId/:qty` - параметры в URL вместо body
- Должно быть `POST /cart/add` с JSON body

## 📊 Статистика проблем

- **Критические проблемы безопасности:** 5
- **Архитектурные проблемы:** 6
- **Проблемы качества кода:** 6
- **Отсутствующие функции:** 8

**Всего найдено:** 25 проблем

## 🎯 Рекомендации по приоритетам

### Высокий приоритет (исправить немедленно):
1. Исправить хардкод userID - использовать данные из JWT
2. Использовать секрет JWT из конфигурации
3. Реализовать реальные подключения к PostgreSQL и Redis
4. Удалить дублирование логики аутентификации
5. Вынести секреты в переменные окружения

### Средний приоритет:
6. Добавить валидацию входных данных
7. Исправить обработку ошибок
8. Удалить неиспользуемый код
9. Исправить порядок middleware
10. Добавить тесты

### Низкий приоритет:
11. Добавить README и документацию
12. Добавить Docker конфигурацию
13. Добавить .gitignore
14. Рефакторинг дублирующегося кода
